name: e-cubed

services:
  web:
    build: .
    ports:
      - "5000:5000"
    env_file:
      - .env
    volumes:
      - ecube-data:/app/instance
    restart: unless-stopped

volumes:
  ecube-data:
